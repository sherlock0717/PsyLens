# 歧义证据复核（AMBIGUOUS_EVIDENCE_REVIEW）

> 2 条 legacy 证据的 unit_text 在公开样本中命中多个候选，来源不确定。
> 本阶段**不自动定案**：`ambiguous_evidence_queue.csv` 保持 `pending_human_resolution`，等待人工确认。
> 下方推荐仅供人工参考，不写入数据文件。

## 1. `83_u2`（unit_text = 「没啥用」）

- legacy parent_id：83
- 候选 clean id：`139`（NGA_0019）、`203`（NGA_0083），candidate_count = 2
- 候选平台：均为 NGA
- 候选原文节选：
  - `139`：「说实话没啥用，加一起就比循环往复强一点……」
  - `203`：「三个金用起来可能不如两个银凑出来的大法师羁绊 没啥用」

分析：`「没啥用」`是极短通用短语，两个候选都包含。仅凭该短语无法唯一确定来源。两个候选都属 NGA、都在讨论羁绊/装备价值，即便定位到任一候选，平台层结论不变（均 NGA），但**具体样本归属仍不确定**。

**推荐（供人工参考，不定案）**：倾向 `203`（NGA_0083），因其句尾「没啥用」与该短语作为独立单元的语气更贴合；但证据不足以排除 `139`。建议人工结合 legacy 证据拆分上下文最终确认。

## 2. `96_u3`（unit_text = 「没了」）

- legacy parent_id：96
- 候选 clean id：`40`(BILI_0040)、`42`(BILI_0042)、`63`(BILI_0063)、`170`(NGA_0050)、`216`(NGA_0096)、`334`(TIEBA_0094)，candidate_count = 6
- 候选平台：Bili / NGA / Tieba（跨三平台）

分析：`「没了」`是极短口语，散布在 6 个样本、跨 3 个平台。**无法从文本唯一定位**，且候选跨平台，连平台层归属都不确定。

**推荐（供人工参考，不定案）**：不建议自动归入任一候选；该单元本身信息量极低（疑似 legacy 证据拆分的过短噪声）。建议人工评估是否应**作废该证据单元**，而非强行归属。

## 3. 处理原则

- 不自动选第一个候选；不机械加 120；不分配平台；
- 两条均保持 `pending_human_resolution`；
- 二者共性：**极短文本**（「没啥用」「没了」），提示 legacy 证据拆分阶段存在过短单元；未来重建证据层时应设最小单元长度阈值以减少此类歧义。
