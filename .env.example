# PsyLens .env 示例
# 默认离线 Demo 不读取本文件、不需要任何密钥。
# 仅当用户显式启用真实 provider（非默认、非 CI）时才可能用到。
# 切勿把真实 Key/Token/Cookie 提交到仓库。

# 真实模型 provider（可选，默认不启用；Demo 默认 --provider mock）
# PSYLENS_LLM_API_KEY=
# PSYLENS_LLM_BASE_URL=
# PSYLENS_LLM_MODEL=

# 真实抓取（可选，默认关闭；CI 永不运行）
# PSYLENS_ENABLE_CRAWLER=false
